#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void) {

  double w, h, area, perimeter;
  
  printf("가로와 세로를 입력하세요: ");
  
  scanf ("%lf %lf", &w, &h);
  
  area = w * h;
  perimeter = 2 * (w + h) ;
  
  printf("사각형의 넓이: %lf\n105, 사각형의 둘레: %lf\n", area, perimeter);
  
  return 0;
}
